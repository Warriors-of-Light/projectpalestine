/** @type {import('next').NextConfig} */

const nextConfig = {
  images: {
    domains: [
      "plchldr.co",
      "img.icons8.com",
      "images.unsplash.com",
      "firebasestorage.googleapis.com",
    ],
  },
};

module.exports = nextConfig;
